<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="kw.kimkihong.assign3">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
            android:name=".retrofit.Request"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.Guest">
        <activity android:name=".activity.ReservDetailActivity">
        </activity>
        <activity android:name=".activity.ReservationActivity">
        </activity>
        <activity android:name=".activity.DetailActivity">
        </activity>
        <activity
                android:name=".util.TimeActivity"
                android:theme="@style/Theme.MaterialComponents.Light.Dialog">
        </activity>
        <activity
                android:name=".util.DateActivity"
                android:theme="@style/Theme.MaterialComponents.Light.Dialog">
        </activity>
        <activity android:name=".util.WebViewActivity">
        </activity>
        <activity android:name=".activity.RegisterActivity">
        </activity>
        <activity android:name=".activity.MainActivity">
        </activity>
        <activity android:name=".activity.LoginActivity">
        </activity>
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
                android:name="com.naver.maps.map.CLIENT_ID"
                android:value="cuonr9xz2i"/>
    </application>

</manifest>